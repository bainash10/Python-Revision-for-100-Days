# Using input() function 
name=input("Enter your name")
age=int(input("Whats your age")) #input always takes string datatype
age = age+1  #If any maths calculation is to be done always typecast
print(f"You are {name} and your age is {age}")
print(f"your age is {age}")